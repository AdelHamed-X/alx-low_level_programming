#include "main.h"

/**
 * print_alphabet - prints alphabet in lowercase followd by a new line.
 *
 */
void print_alphabet(void)
{
	for (char i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
